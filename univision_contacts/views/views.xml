<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="univision_contacts.list">
      <field name="name">univision_contacts list</field>
      <field name="model">univision_contacts.univision_contacts</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="univision_contacts.action_window">
      <field name="name">univision_contacts window</field>
      <field name="res_model">univision_contacts.univision_contacts</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="univision_contacts.action_server">
      <field name="name">univision_contacts server</field>
      <field name="model_id" ref="model_univision_contacts_univision_contacts"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="univision_contacts" id="univision_contacts.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="univision_contacts.menu_1" parent="univision_contacts.menu_root"/>
    <menuitem name="Menu 2" id="univision_contacts.menu_2" parent="univision_contacts.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="univision_contacts.menu_1_list" parent="univision_contacts.menu_1"
              action="univision_contacts.action_window"/>
    <menuitem name="Server to list" id="univision_contacts" parent="univision_contacts.menu_2"
              action="univision_contacts.action_server"/>
    -->
  </data>
</odoo>