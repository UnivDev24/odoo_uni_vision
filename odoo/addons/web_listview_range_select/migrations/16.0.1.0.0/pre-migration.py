import logging
_logger = logging.getLogger(__name__)

def migrate(cr, version):

    _logger.info('Web list view range : pre-migration start')

    _logger.info('Web list view range : pre-migration end')