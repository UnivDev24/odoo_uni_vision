import logging
_logger = logging.getLogger(__name__)

def migrate(cr, version):

    _logger.info('Web tree resize column : pre-migration start')

    _logger.info('Web tree resize column : pre-migration end')